version: '2'

services:
  dependency-graph-jetty:
    image: tangobee/akto-dependency-graph
    restart: always
    ports:
      - "80:8080"

  dependency-graph-validator:
    image: tangobee/akto-dependency-graph-validator:latest
    restart: always
    ports:
      - "81:3001"