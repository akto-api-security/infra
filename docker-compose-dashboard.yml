version: '3.3'

services:
  akto-api-security-dashboard:
    image: aktosecurity/akto-api-security-dashboard:latest
    env_file: ./docker.env
    restart: always
    ports: 
      - "8080:8080"
    depends_on:
      - mongo